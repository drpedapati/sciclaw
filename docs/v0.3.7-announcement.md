# sciClaw v0.3.7 Announcement

## Tweet

sciClaw v0.3.7 is out.

Default model is now gpt-5.6-sol @ low. Codex streaming is tougher (delta fallback, clearer errors, no silent model swaps). Soft incomplete turns still show the human reply, but agent -m keeps a non-zero exit when work is unfinished.

brew update && brew upgrade sciclaw
sciclaw service refresh

https://github.com/drpedapati/sciclaw/releases/tag/v0.3.7

---

## Short notes

- Product default: `gpt-5.6-sol` + `reasoning_effort: low`
- Codex: delta-text fallback, openai-go 3.22.0, resolveCodexModel remaps gpt-5.2 / rejects wrong-family
- Incomplete-turn CLI: show status, exit ≠0 on one-shot; Error() stays short for ops
- Includes Jul 11 AGENTS ImageMagick contact-sheet guidance
