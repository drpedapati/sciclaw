# sciClaw v0.3.8 Announcement

## Tweet

sciClaw v0.3.8 is out.

Codex ChatGPT OAuth now sends the client identity headers Luna needs, so gpt-5.6-luna stops 404ing. Catalog lists Luna next to Sol/Terra; default stays gpt-5.6-sol @ low.

brew update && brew upgrade sciclaw
sciclaw service refresh

https://github.com/drpedapati/sciclaw/releases/tag/v0.3.8

---

## Short notes

- Codex OAuth: `originator=codex_cli_rs`, `version=0.144.1`, versioned User-Agent (catalog `minimal_client_version` for GPT-5.6)
- Catalog: add `gpt-5.6-luna`; default remains Sol @ low
- PR: https://github.com/drpedapati/sciclaw/pull/126
